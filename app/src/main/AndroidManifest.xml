<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.laundryapp">

    <application
        android:allowBackup="true"
        android:label="LaundryAPP"
        android:icon="@mipmap/logo"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaundryAPP">

        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.order.SelectCustomerActivity" />
        <activity android:name=".ui.order.NewOrderActivity" />
        <activity android:name=".ui.order.PaymentActivity" />

        <activity android:name=".ui.order.OrderDetailActivity" android:exported="false" />

        <!-- CRUD Customer -->
        <activity android:name=".ui.customer.CustomerListActivity" />
        <activity android:name=".ui.customer.CustomerFormActivity" />

        <!-- CRUD Service -->
        <activity android:name=".ui.service.ServiceListActivity" />
        <activity android:name=".ui.service.ServiceFormActivity" />

        <!-- Report -->
        <activity android:name=".ui.report.ReportActivity" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

    </application>

</manifest>
